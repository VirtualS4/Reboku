<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.reboku">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logoketumbarbubuk"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logoketumbarbubuk"
        android:supportsRtl="true"
        android:theme="@style/Theme.Reboku">
        <activity android:name=".AboutUs"></activity>
        <activity android:name=".News" />
        <activity android:name=".MyPlant" />
        <activity android:name=".InformationCenter" />
        <activity android:name=".ProgressReboisasi" />
        <activity android:name=".MainMenu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>